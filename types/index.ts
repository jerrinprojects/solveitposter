export interface PosterSkill {
  code: string;
  description: string;
  imageUrl?: string;
}

export interface PosterMeta {
  subject: string;
  phase: string;
  year: string;
  theme: string;
  brand: string;
}

export interface FooterData {
  brand: string;
}
